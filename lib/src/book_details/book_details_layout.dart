import 'package:flutter/material.dart';

class BookDetailsLayout extends StatelessWidget {
  const BookDetailsLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
